#端口号
server:
  port: 8082
ribbon:
  ConnectTimeout: 12000
  ReadTimeout: 8000

spring:
  application:
    name: ums-service
  cloud:
    nacos:
      config:
        #nacos做配置中心的IP地址
        server-addr: 127.0.0.1:8848
